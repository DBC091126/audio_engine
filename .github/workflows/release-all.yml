name: release-all

on:
  workflow_dispatch:

env:
  JAVAFX_VERSION: 23.0.2

permissions:
  contents: write

jobs:
  unix:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            rust_target: x86_64-unknown-linux-gnu
            arch: x86_64
            suffix: linux-x86_64
          - os: ubuntu-24.04-arm
            rust_target: aarch64-unknown-linux-gnu
            arch: arm64
            suffix: linux-arm64
          - os: macos-15-intel
            rust_target: x86_64-apple-darwin
            arch: x86_64
            suffix: macos-x86_64
          - os: macos-14
            rust_target: aarch64-apple-darwin
            arch: arm64
            suffix: macos-arm64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust_target }}

      - uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ~/.m2/repository
          key: ${{ runner.os }}-cargo-maven-${{ hashFiles('**/Cargo.lock', '**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-maven-

      - name: Install native build dependencies
        shell: bash
        run: |
          if [[ "${{ matrix.os }}" == ubuntu-* ]]; then
            sudo apt-get update
            sudo apt-get install -y \
              pkg-config libavformat-dev libavcodec-dev libavutil-dev \
              libavfilter-dev libswresample-dev libswscale-dev libavdevice-dev
          else
            brew install ffmpeg pkg-config
          fi

      - name: Build installer
        env:
          GUI_JAVA_HOME: ${{ env.JAVA_HOME }}
          RUST_TARGET: ${{ matrix.rust_target }}
          MAVEN_CMD: ${{ github.workspace }}/gui/mvnw
        shell: bash
        run: bash scripts/package.sh

      - name: Upload release asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          ls -lh dist
          package=$(find dist -maxdepth 1 -type f | head -n 1)
          test -n "$package"
          if [[ "${{ matrix.os }}" == macos-* ]]; then
            ext="${package##*.}"
            renamed="dist/audio-engine_1.2.1_${{ matrix.suffix }}.$ext"
            mv "$package" "$renamed"
            package="$renamed"
          fi
          gh release upload v1.2.1 "$package" \
            --repo DBC091126/audio_engine --clobber
          checksum="$package.sha256"
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "$package" > "$checksum"
          elif command -v shasum >/dev/null 2>&1; then
            shasum -a 256 "$package" > "$checksum"
          fi
          if [ -f "$checksum" ]; then
            gh release upload v1.2.1 "$checksum" \
              --repo DBC091126/audio_engine --clobber
          fi

  windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: >-
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-ninja
            mingw-w64-x86_64-pkgconf
            mingw-w64-x86_64-clang
            mingw-w64-x86_64-ffmpeg

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-gnu

      - uses: actions/cache@v4
        with:
          path: |
            C:\Users\runneradmin\.cargo\registry
            C:\Users\runneradmin\.cargo\git
            C:\Users\runneradmin\.m2\repository
          key: ${{ runner.os }}-cargo-maven-${{ hashFiles('**/Cargo.lock', '**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-maven-

      - name: Remove Linux-only Cargo environment
        run: rm -f .cargo/config.toml

      - name: Build Rust engine for Windows
        shell: msys2 {0}
        env:
          CC: gcc
          CXX: g++
          AR: ar
          CMAKE_POLICY_VERSION_MINIMUM: "3.5"
          LIBCLANG_PATH: /mingw64/bin
          BINDGEN_EXTRA_CLANG_ARGS: "-I/mingw64/include"
        run: |
          export PATH="/c/Users/runneradmin/.cargo/bin:/mingw64/bin:/usr/bin:$PATH"
          export PKG_CONFIG_PATH=/mingw64/lib/pkgconfig
          export FFMPEG_DIR=/mingw64
          cargo build --release --target x86_64-pc-windows-gnu

      - name: Build Java GUI distribution
        run: |
          cd gui
          ./mvnw -q -DskipTests package \
            -Djavafx.version="$JAVAFX_VERSION" \
            -Daudio.engine.native.dir="$PWD/../target/x86_64-pc-windows-gnu/release"

      - name: Build Windows installer
        run: |
          "$JAVA_HOME/bin/jpackage" \
            --type exe \
            --input gui/target/audio-engine-gui-1.2.1-dist \
            --dest dist \
            --name AudioEngine \
            --app-version 1.2.1 \
            --main-jar audio-engine-gui-1.2.1.jar \
            --main-class com.losshifi.audioengine.Main \
            --module-path gui/target/audio-engine-gui-1.2.1-dist \
            --add-modules javafx.controls,java.logging,java.naming \
            --win-menu \
            --win-shortcut

      - name: Upload Windows installer
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          ls -lh dist
          installer=$(find dist -maxdepth 1 -name '*.exe' -print -quit)
          test -n "$installer"
          gh release upload v1.2.1 "$installer" \
            --repo DBC091126/audio_engine --clobber
          checksum="$installer.sha256"
          if command -v sha256sum >/dev/null 2>&1; then
            sha256sum "$installer" > "$checksum"
          elif command -v shasum >/dev/null 2>&1; then
            shasum -a 256 "$installer" > "$checksum"
          fi
          if [ -f "$checksum" ]; then
            gh release upload v1.2.1 "$checksum" \
              --repo DBC091126/audio_engine --clobber
          fi
